import React from "react";

const HomeJSX = (props) => {
    // console.log(props.stock);
    // console.log(typeof props.timeStamp);
    // let symbol = "";
    // let timeArray = [];

    // for (const [key, value] of Object.entries(props.stock)) {
    //     if(key.startsWith("2."))
    //     {
    //         symbol = value;
    //     }
    //     console.log(key);
    // }
    // for (const [key, value] of Object.entries(props.timeStamp)) {
    //         timeArray.push(value);
    // }


    return (
        <>
        <h1>Home</h1>
        </>
    )
}

export default HomeJSX;
